#include<iostream>
using namespace std;
struct node{
    int l;
    int r;
    int val;
}tree[2000005];
int a[100005];
void build(int pos,int l,int r){
    tree[pos].l=l,tree[pos].r=r,tree[pos].val=0;
    if(l==r){
        tree[pos].val=
    }
}
int main(){
    
}